<UserControl x:Class="Quản_lý_công_tơ_điện.BM3"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Quản_lý_công_tơ_điện"
             mc:Ignorable="d" 
             d:DesignHeight="700" d:DesignWidth="1000" Background="White">
    <UserControl.Resources>
        <Style x:Key="CenterText" TargetType="TextBlock">
            <Setter Property="HorizontalAlignment" Value="Center"/>
            <Setter Property="VerticalAlignment" Value="Center"/>
            <Setter Property="TextWrapping" Value="Wrap"/>
        </Style>

        <Style x:Key="VerticalCenterText" TargetType="TextBlock">
            <Setter Property="VerticalAlignment" Value="Center"/>
            <Setter Property="Margin" Value="10,0,10,0"/>
            <Setter Property="TextWrapping" Value="Wrap"/>
        </Style>

        <Style TargetType="TextBox">
            <Setter Property="Height" Value="32"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
            <Setter Property="Padding" Value="8,0"/>
            <Setter Property="BorderBrush" Value="#ced4da"/>
            <Setter Property="BorderThickness" Value="1"/>
        </Style>

        <Style TargetType="ComboBox">
            <Setter Property="Height" Value="32"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
            <Setter Property="BorderBrush" Value="#ced4da"/>
        </Style>

        <Style TargetType="DatePicker">
            <Setter Property="Height" Value="32"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
        </Style>

        <Style TargetType="DataGridColumnHeader">
            <Setter Property="Background" Value="#e8f0fe"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="Foreground" Value="#3b5998"/>
            <Setter Property="MinHeight" Value="40"/>
            <Setter Property="HorizontalContentAlignment" Value="Center"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
            <Setter Property="BorderBrush" Value="#ced4da"/>
            <Setter Property="BorderThickness" Value="0,0,1,1"/>

            <Setter Property="ContentTemplate">
                <Setter.Value>
                    <DataTemplate>
                        <TextBlock Text="{Binding}" TextWrapping="Wrap" TextAlignment="Center"/>
                    </DataTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </UserControl.Resources>

    <Grid Margin="30,20,30,20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" Margin="0,0,0,20">
            <TextBlock Text="Tra cứu đề nghị cấp điện" FontSize="26" FontWeight="Bold" Foreground="#3b5998" Margin="0,0,0,10"/>
        </StackPanel>

        <Border Grid.Row="1" Background="#f8f9fa" BorderBrush="#e9ecef" BorderThickness="1" CornerRadius="8" Padding="20" Margin="0,0,0,20">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="20"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="1*"/>
                    <ColumnDefinition Width="1*"/>
                    <ColumnDefinition Width="1*"/>
                    <ColumnDefinition Width="1*"/>
                    <ColumnDefinition Width="1*"/>
                    <ColumnDefinition Width="1*"/>
                </Grid.ColumnDefinitions>

                <StackPanel Grid.Row="0" Grid.Column="0" Margin="0,0,15,0">
                    <TextBlock Text="Mã phiếu cấp điện" FontWeight="SemiBold" Foreground="Gray" Margin="0,0,0,6"/>
                    <TextBox x:Name="txtMaPhieu"/>
                </StackPanel>

                <StackPanel Grid.Row="0" Grid.Column="1" Margin="0,0,15,0">
                    <TextBlock Text="Họ tên khách hàng" FontWeight="SemiBold" Foreground="Gray" Margin="0,0,0,6"/>
                    <TextBox x:Name="txtHoTen"/>
                </StackPanel>

                <StackPanel Grid.Row="0" Grid.Column="2" Margin="0,0,15,0">
                    <TextBlock Text="Địa chỉ đăng ký" FontWeight="SemiBold" Foreground="Gray" Margin="0,0,0,6"/>
                    <TextBox x:Name="txtDiaChi"/>
                </StackPanel>

                <StackPanel Grid.Row="0" Grid.Column="3" Margin="0,0,15,0">
                    <TextBlock Text="Số pha đăng ký" FontWeight="SemiBold" Foreground="Gray" Margin="0,0,0,6"/>
                    <ComboBox x:Name="cboSoPha"/>
                </StackPanel>

                <StackPanel Grid.Row="0" Grid.Column="4" Margin="0,0,15,0">
                    <TextBlock Text="Mục đích sử dụng" FontWeight="SemiBold" Foreground="Gray" Margin="0,0,0,6"/>
                    <ComboBox x:Name="cboMucDich"/>
                </StackPanel>

                <StackPanel Grid.Row="0" Grid.Column="5">
                    <TextBlock Text="Trạng thái" FontWeight="SemiBold" Foreground="Gray" Margin="0,0,0,6"/>
                    <TextBox x:Name="txtTrangThai"/>
                </StackPanel>

                <Grid Grid.Row="2" Grid.ColumnSpan="6">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>

                    <StackPanel Grid.Column="0" Orientation="Horizontal">
                        <TextBlock Text="Thời gian gửi yêu cầu" FontWeight="SemiBold" Foreground="Gray" Margin="0,0,15,0" VerticalAlignment="Center"/>

                        <TextBlock Text="từ" FontWeight="Bold" Foreground="#e74c3c" Margin="0,0,10,0" VerticalAlignment="Center"/>
                        <DatePicker x:Name="dtpTuNgay" Width="140" Margin="0,0,30,0"/>

                        <TextBlock Text="đến" FontWeight="Bold" Foreground="#e74c3c" Margin="0,0,10,0" VerticalAlignment="Center"/>
                        <DatePicker x:Name="dtpDenNgay" Width="140"/>
                    </StackPanel>

                    <StackPanel Grid.Column="1" Orientation="Horizontal" HorizontalAlignment="Right">
                        <Button x:Name="btnTraCuu" Content="TRA CỨU" Width="100" Height="32" Background="#4e73df" Foreground="White" BorderThickness="0" FontWeight="Bold" Click="btnTraCuu_Click" Margin="0,0,15,0"/>
                        <Button x:Name="btnXoaLoc" Content="XÓA BỘ LỌC" Width="100" Height="32" Background="White" BorderBrush="#adb5bd" BorderThickness="1" Foreground="#495057" FontWeight="Bold" Click="btnXoaLoc_Click"/>
                    </StackPanel>
                </Grid>
            </Grid>
        </Border>

        <DataGrid x:Name="dgvKetQua" Grid.Row="2" 
                  AutoGenerateColumns="False" 
                  IsReadOnly="True" 
                  HeadersVisibility="Column"
                  GridLinesVisibility="Horizontal" 
                  HorizontalGridLinesBrush="#e0e0e0"
                  Background="White" 
                  BorderBrush="#ced4da" 
                  MinRowHeight="42"
                  AlternatingRowBackground="#f8f9fa"
                  VerticalContentAlignment="Center">
            <DataGrid.Columns>
                <DataGridTextColumn Header="STT" Binding="{Binding STT}" Width="50" ElementStyle="{StaticResource CenterText}"/>
                <DataGridTextColumn Header="THỜI GIAN GỬI" Binding="{Binding ThoiGianGui}" Width="150" ElementStyle="{StaticResource VerticalCenterText}"/>
                <DataGridTextColumn Header="HỌ TÊN KHÁCH HÀNG" Binding="{Binding HoTen}" Width="1.5*" ElementStyle="{StaticResource VerticalCenterText}"/>
                <DataGridTextColumn Header="ĐỊA CHỈ" Binding="{Binding DiaChi}" Width="2*" ElementStyle="{StaticResource VerticalCenterText}"/>
                <DataGridTextColumn Header="SỐ PHA" Binding="{Binding SoPha}" Width="100" ElementStyle="{StaticResource CenterText}"/>
                <DataGridTextColumn Header="MỤC ĐÍCH" Binding="{Binding MucDich}" Width="1.2*" ElementStyle="{StaticResource VerticalCenterText}"/>

                <DataGridTextColumn Header="TRẠNG THÁI" Binding="{Binding TrangThai}" Width="130">
                    <DataGridTextColumn.ElementStyle>
                        <Style TargetType="TextBlock" BasedOn="{StaticResource VerticalCenterText}">
                            <Setter Property="FontWeight" Value="SemiBold"/>
                        </Style>
                    </DataGridTextColumn.ElementStyle>
                </DataGridTextColumn>
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</UserControl>