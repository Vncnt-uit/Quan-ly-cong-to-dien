<UserControl x:Class="Quản_lý_công_tơ_điện.Views.ThongKeDien"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="850" d:DesignWidth="1100" 
             Background="Transparent">

    <UserControl.Resources>
        <BooleanToVisibilityConverter x:Key="BoolToVis"/>

        <Style x:Key="CenterText" TargetType="TextBlock">
            <Setter Property="HorizontalAlignment" Value="Center"/>
            <Setter Property="VerticalAlignment" Value="Center"/>
            <Setter Property="TextWrapping" Value="Wrap"/>
        </Style>

        <Style TargetType="DataGridRow">
            <Setter Property="Background" Value="White"/>
            <Style.Resources>
                <SolidColorBrush x:Key="{x:Static SystemColors.HighlightBrushKey}" Color="#cbd5e1"/>
                <SolidColorBrush x:Key="{x:Static SystemColors.InactiveSelectionHighlightBrushKey}" Color="#cbd5e1"/>
                <SolidColorBrush x:Key="{x:Static SystemColors.HighlightTextBrushKey}" Color="Black"/>
            </Style.Resources>
        </Style>

        <Style TargetType="DataGridColumnHeader">
            <Setter Property="Background" Value="#d6e4ff"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="Foreground" Value="#3b5998"/>
            <Setter Property="MinHeight" Value="45"/>
            <Setter Property="HorizontalContentAlignment" Value="Center"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
            <Setter Property="BorderBrush" Value="#bacdf2"/>
            <Setter Property="BorderThickness" Value="0,0,1,1"/>
            <Setter Property="Padding" Value="5"/>
            <Setter Property="ContentTemplate">
                <Setter.Value>
                    <DataTemplate>
                        <TextBlock Text="{Binding}" TextWrapping="Wrap" TextAlignment="Center"/>
                    </DataTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="FormLabel" TargetType="TextBlock">
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Foreground" Value="#475569"/>
            <Setter Property="FontSize" Value="12"/>
            <Setter Property="Margin" Value="5,0,0,5"/>
        </Style>
    </UserControl.Resources>

    <Grid VerticalAlignment="Center" Margin="30">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" Margin="0,0,0,25" HorizontalAlignment="Center">
            <TextBlock FontSize="36" FontWeight="Bold" HorizontalAlignment="Center" Margin="0,0,0,10">
                <TextBlock.Foreground>
                    <LinearGradientBrush StartPoint="0.5,0" EndPoint="0.5,1">
                        <GradientStop Color="#6b87f2" Offset="0"/>
                        <GradientStop Color="#88a1f4" Offset="0.6"/>
                        <GradientStop Color="#c3caf8" Offset="1"/>
                    </LinearGradientBrush>
                </TextBlock.Foreground>
                <Run Text="Thống kê sản lượng điện tiêu thụ"/>
            </TextBlock>
            <TextBlock Text="Theo dõi, đối chiếu và phân tích biến động sản lượng điện tiêu thụ qua các tháng trong năm." 
                       FontSize="15" Foreground="#555555" HorizontalAlignment="Center" FontWeight="SemiBold"/>
        </StackPanel>

        <Border Grid.Row="1" Background="#60FFFFFF" BorderBrush="#ffffff" BorderThickness="2" CornerRadius="15" Padding="30">
            <Border.Effect>
                <DropShadowEffect Color="#000000" Direction="270" ShadowDepth="4" BlurRadius="20" Opacity="0.08"/>
            </Border.Effect>

            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <Grid Grid.Row="0" Margin="0,0,0,20">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>

                    <TextBlock Grid.Column="0" Text="Tùy chọn thống kê" FontSize="18" FontWeight="Bold" Foreground="#3b5998"/>

                    <TextBlock Grid.Column="1" Text="{Binding StatusMessage}" HorizontalAlignment="Center" VerticalAlignment="Center" FontSize="16" FontStyle="Italic" FontWeight="Bold">
                        <TextBlock.Style>
                            <Style TargetType="TextBlock">
                                <Setter Property="Foreground" Value="#e74a3b"/>
                                <Style.Triggers>
                                    <DataTrigger Binding="{Binding IsSuccessStatus}" Value="True">
                                        <Setter Property="Foreground" Value="#1cc88a"/>
                                    </DataTrigger>
                                </Style.Triggers>
                            </Style>
                        </TextBlock.Style>
                    </TextBlock>
                </Grid>

                <Grid Grid.Row="1" Margin="0,0,0,20">
                    <StackPanel HorizontalAlignment="Left" Width="250">
                        <TextBlock Text="Năm thống kê" Style="{StaticResource FormLabel}" HorizontalAlignment="Center"/>

                        <Border CornerRadius="16" Height="34" Background="White" BorderBrush="#cbd5e1" BorderThickness="1" Margin="0,0,0,2" Padding="3,2">
                            <Border.Style>
                                <Style TargetType="Border">
                                    <Style.Triggers>
                                        <DataTrigger Binding="{Binding HasNamError}" Value="True">
                                            <Setter Property="BorderBrush" Value="Red"/>
                                        </DataTrigger>
                                    </Style.Triggers>
                                </Style>
                            </Border.Style>

                            <ComboBox ItemsSource="{Binding DanhSachNam}" 
                                      SelectedItem="{Binding SelectedNam}" 
                                      Background="Transparent" BorderThickness="0" VerticalContentAlignment="Center" Margin="5,0"
                                      IsEditable="True" IsReadOnly="True" Cursor="Arrow" ForceCursor="True">
                                <ComboBox.Resources>
                                    <Style TargetType="TextBox">
                                        <Setter Property="Cursor" Value="Arrow"/>
                                        <Setter Property="IsReadOnlyCaretVisible" Value="False"/>
                                        <Setter Property="ForceCursor" Value="True"/>
                                    </Style>
                                </ComboBox.Resources>
                            </ComboBox>
                        </Border>

                        <TextBlock Text="{Binding NamErrorMessage}" Foreground="#e74a3b" FontSize="11" FontStyle="Italic" 
                                   Visibility="{Binding HasNamError, Converter={StaticResource BoolToVis}}" Margin="10,0,0,10" MinHeight="15"/>
                    </StackPanel>
                </Grid>

                <Grid Grid.Row="2">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>

                    <TextBlock Grid.Row="0" Text="Chi tiết sản lượng" FontSize="16" FontWeight="Bold" Foreground="#1e293b" Margin="0,0,0,10"/>

                    <Border Grid.Row="1" BorderBrush="#bacdf2" BorderThickness="1" CornerRadius="12" VerticalAlignment="Top">
                        <Grid>
                            <Border x:Name="MaskBM6" Background="White" CornerRadius="11"/>
                            <Grid>
                                <Grid.OpacityMask>
                                    <VisualBrush Visual="{Binding ElementName=MaskBM6}" Stretch="None" AlignmentX="Left" AlignmentY="Top"/>
                                </Grid.OpacityMask>

                                <Border Background="White"/>
                                <Border Height="46" Background="#d6e4ff" VerticalAlignment="Top" CornerRadius="11,11,0,0"/>

                                <DataGrid ItemsSource="{Binding DanhSachThongKe}" 
                                          AutoGenerateColumns="False" IsReadOnly="True" HeadersVisibility="Column"
                                          GridLinesVisibility="All" HorizontalGridLinesBrush="#bacdf2" VerticalGridLinesBrush="#bacdf2"
                                          Background="Transparent" BorderThickness="0" 
                                          RowHeight="40" AlternatingRowBackground="#eff4fd" VerticalContentAlignment="Center" 
                                          HorizontalScrollBarVisibility="Disabled" 
                                          VirtualizingPanel.ScrollUnit="Pixel">

                                    <DataGrid.Columns>
                                        <DataGridTextColumn Header="STT" Binding="{Binding STT}" Width="1*" ElementStyle="{StaticResource CenterText}"/>
                                        <DataGridTextColumn Header="THÁNG" Binding="{Binding ThangText}" Width="2*" ElementStyle="{StaticResource CenterText}"/>

                                        <DataGridTextColumn Header="SẢN LƯỢNG" Binding="{Binding SanLuong, StringFormat=\{0:N0\}}" Width="2*">
                                            <DataGridTextColumn.ElementStyle>
                                                <Style TargetType="TextBlock" BasedOn="{StaticResource CenterText}">
                                                    <Setter Property="FontWeight" Value="SemiBold"/>
                                                </Style>
                                            </DataGridTextColumn.ElementStyle>
                                        </DataGridTextColumn>

                                        <DataGridTextColumn Header="SO VỚI THÁNG TRƯỚC" Binding="{Binding SoVoiThangTruoc}" Width="2*">
                                            <DataGridTextColumn.ElementStyle>
                                                <Style TargetType="TextBlock" BasedOn="{StaticResource CenterText}">
                                                    <Setter Property="FontWeight" Value="Bold"/>
                                                </Style>
                                            </DataGridTextColumn.ElementStyle>
                                        </DataGridTextColumn>
                                    </DataGrid.Columns>
                                </DataGrid>
                            </Grid>
                        </Grid>
                    </Border>
                </Grid>

                <StackPanel Grid.Row="3" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,20,0,0">
                    <Button Content="ĐẶT LẠI NĂM" Command="{Binding ResetNamCommand}" FontWeight="Bold" Width="120" Height="40" Background="White" Margin="0,0,15,0" BorderThickness="0" Cursor="Hand">
                        <Button.Resources>
                            <Style TargetType="Border">
                                <Setter Property="CornerRadius" Value="20"/>
                            </Style>
                        </Button.Resources>
                        <Button.Effect>
                            <DropShadowEffect Color="Black" Direction="270" ShadowDepth="2" BlurRadius="10" Opacity="0.1"/>
                        </Button.Effect>
                    </Button>

                    <Button Content="📊 LẬP THỐNG KÊ" Command="{Binding ThongKeCommand}" FontWeight="Bold" Width="150" Height="40" Background="#2563eb" Foreground="White" BorderThickness="0" Cursor="Hand">
                        <Button.Resources>
                            <Style TargetType="Border">
                                <Setter Property="CornerRadius" Value="20"/>
                            </Style>
                        </Button.Resources>
                        <Button.Effect>
                            <DropShadowEffect Color="#2563eb" Direction="270" ShadowDepth="3" BlurRadius="12" Opacity="0.4"/>
                        </Button.Effect>
                    </Button>
                </StackPanel>

            </Grid>
        </Border>
    </Grid>
</UserControl>