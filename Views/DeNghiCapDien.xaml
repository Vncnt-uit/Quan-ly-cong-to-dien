<UserControl x:Class="Quản_lý_công_tơ_điện.Views.DeNghiCapDien"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="750" d:DesignWidth="950"
             Background="Transparent">

    <UserControl.Resources>
        <BooleanToVisibilityConverter x:Key="BoolToVis"/>
        <Style x:Key="NoCursorTextBox" TargetType="TextBox">
            <Setter Property="Cursor" Value="Arrow"/>
        </Style>
    </UserControl.Resources>

    <Grid VerticalAlignment="Center" Margin="0,0,0,40">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" Margin="0,0,0,20" HorizontalAlignment="Center">
            <TextBlock FontSize="32" FontWeight="Bold" HorizontalAlignment="Center" Margin="0,0,0,5">
                <TextBlock.Foreground>
                    <LinearGradientBrush StartPoint="0.5,0" EndPoint="0.5,1">
                        <GradientStop Color="#6b87f2" Offset="0"/>
                        <GradientStop Color="#88a1f4" Offset="0.6"/>
                        <GradientStop Color="#c3caf8" Offset="1"/>
                    </LinearGradientBrush>
                </TextBlock.Foreground>
                <Run Text="Tiếp nhận đề nghị cấp điện"/>
            </TextBlock>
            <TextBlock Text="Vui lòng cung cấp đầy đủ thông tin để tiến hành yêu cầu cấp điện." FontSize="14" Foreground="Black" HorizontalAlignment="Center" FontWeight="SemiBold"/>
        </StackPanel>

        <Grid Grid.Row="1" Margin="20">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="1*"/>
                <ColumnDefinition Width="1*"/>
            </Grid.ColumnDefinitions>

            <StackPanel Grid.Column="0" Margin="0,0,10,0">
                <Grid>
                    <Grid.Effect>
                        <DropShadowEffect Color="#FFFFFF" Direction="315" ShadowDepth="2" BlurRadius="15" Opacity="0.15"/>
                    </Grid.Effect>

                    <Border Background="#60FFFFFF" BorderBrush="White" BorderThickness="2" CornerRadius="15" Padding="15">
                        <StackPanel>
                            <TextBlock Text="Thông tin khách hàng" FontSize="20" FontWeight="Bold" Foreground="#3b5998" Margin="0,0,0,15"/>

                            <StackPanel Margin="0,0,0,15">
                                <TextBlock Text="SỐ CCCD" FontWeight="SemiBold" Margin="0,0,0,5" Foreground="Black" FontSize="13"/>
                                <Border CornerRadius="12.5" Height="30" Background="White">
                                    <Border.Style>
                                        <Style TargetType="Border">
                                            <Setter Property="BorderBrush" Value="#FFDDDDDD"/>
                                            <Setter Property="BorderThickness" Value="1"/>
                                            <Style.Triggers>
                                                <DataTrigger Binding="{Binding HasCccdError}" Value="True">
                                                    <Setter Property="BorderBrush" Value="Red"/>
                                                    <Setter Property="BorderThickness" Value="1"/>
                                                </DataTrigger>
                                            </Style.Triggers>
                                        </Style>
                                    </Border.Style>
                                    <TextBox Text="{Binding Cccd, UpdateSourceTrigger=LostFocus}" Background="Transparent" BorderThickness="0" VerticalContentAlignment="Center" Padding="8,0,8,0"/>
                                </Border>
                                <TextBlock Text="{Binding CccdErrorMessage}" Foreground="Red" FontSize="11" FontStyle="Italic" Visibility="{Binding HasCccdError, Converter={StaticResource BoolToVis}}" Margin="5,2,0,0" TextWrapping="Wrap"/>
                            </StackPanel>

                            <StackPanel Margin="0,0,0,15">
                                <TextBlock Text="HỌ VÀ TÊN" FontWeight="SemiBold" Margin="0,0,0,5" Foreground="Black" FontSize="13"/>
                                <Border CornerRadius="12.5" Height="30" Background="White">
                                    <Border.Style>
                                        <Style TargetType="Border">
                                            <Setter Property="BorderBrush" Value="#FFDDDDDD"/>
                                            <Setter Property="BorderThickness" Value="1"/>
                                            <Style.Triggers>
                                                <DataTrigger Binding="{Binding HasHoTenError}" Value="True">
                                                    <Setter Property="BorderBrush" Value="Red"/>
                                                    <Setter Property="BorderThickness" Value="1"/>
                                                </DataTrigger>
                                            </Style.Triggers>
                                        </Style>
                                    </Border.Style>
                                    <TextBox Text="{Binding HoTen, UpdateSourceTrigger=LostFocus}" Background="Transparent" BorderThickness="0" VerticalContentAlignment="Center" Padding="8,0,8,0"/>
                                </Border>
                                <TextBlock Text="{Binding HoTenErrorMessage}" Foreground="Red" FontSize="11" FontStyle="Italic" Visibility="{Binding HasHoTenError, Converter={StaticResource BoolToVis}}" Margin="5,2,0,0" TextWrapping="Wrap"/>
                            </StackPanel>

                            <Grid Margin="0,0,0,15">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="1*"/>
                                    <ColumnDefinition Width="1*"/>
                                </Grid.ColumnDefinitions>

                                <StackPanel Grid.Column="0" Margin="0,0,10,0">
                                    <TextBlock Text="NĂM SINH" FontWeight="SemiBold" Margin="0,0,0,5" Foreground="Black" FontSize="13"/>

                                    <Border CornerRadius="12.5" Height="30" Background="White">
                                        <Border.Style>
                                            <Style TargetType="Border">
                                                <Setter Property="BorderBrush" Value="#FFDDDDDD"/>
                                                <Setter Property="BorderThickness" Value="1"/>
                                                <Style.Triggers>
                                                    <DataTrigger Binding="{Binding HasNamSinhError}" Value="True">
                                                        <Setter Property="BorderBrush" Value="Red"/>
                                                        <Setter Property="BorderThickness" Value="1"/>
                                                    </DataTrigger>
                                                </Style.Triggers>
                                            </Style>
                                        </Border.Style>
                                        <TextBox Text="{Binding NamSinh, UpdateSourceTrigger=LostFocus, TargetNullValue=''}" Background="Transparent" BorderThickness="0" VerticalContentAlignment="Center" Padding="8,0,8,0"/>
                                    </Border>

                                    <TextBlock Text="{Binding NamSinhErrorMessage}" Foreground="Red" FontSize="11" FontStyle="Italic" Visibility="{Binding HasNamSinhError, Converter={StaticResource BoolToVis}}" Margin="5,2,0,0" TextWrapping="Wrap"/>
                                </StackPanel>

                                <StackPanel Grid.Column="1" Margin="10,0,0,0">
                                    <TextBlock Text="SỐ ĐIỆN THOẠI" FontWeight="SemiBold" Margin="0,0,0,5" Foreground="Black" FontSize="13"/>
                                    <Border CornerRadius="12.5" Height="30" Background="White">
                                        <Border.Style>
                                            <Style TargetType="Border">
                                                <Setter Property="BorderBrush" Value="#FFDDDDDD"/>
                                                <Setter Property="BorderThickness" Value="1"/>
                                                <Style.Triggers>
                                                    <DataTrigger Binding="{Binding HasSdtError}" Value="True">
                                                        <Setter Property="BorderBrush" Value="Red"/>
                                                        <Setter Property="BorderThickness" Value="1"/>
                                                    </DataTrigger>
                                                </Style.Triggers>
                                            </Style>
                                        </Border.Style>
                                        <TextBox Text="{Binding SoDienThoai, UpdateSourceTrigger=LostFocus}" Background="Transparent" BorderThickness="0" VerticalContentAlignment="Center" Padding="8,0,8,0"/>
                                    </Border>
                                    <TextBlock Text="{Binding SdtErrorMessage}" Foreground="Red" FontSize="11" FontStyle="Italic" Visibility="{Binding HasSdtError, Converter={StaticResource BoolToVis}}" Margin="5,2,0,0" TextWrapping="Wrap"/>
                                </StackPanel>
                            </Grid>

                            <StackPanel Margin="0,0,0,5">
                                <TextBlock Text="EMAIL" FontWeight="SemiBold" Margin="0,0,0,5" Foreground="Black" FontSize="13"/>
                                <Border CornerRadius="12.5" Height="30" Background="White">
                                    <Border.Style>
                                        <Style TargetType="Border">
                                            <Setter Property="BorderBrush" Value="#FFDDDDDD"/>
                                            <Setter Property="BorderThickness" Value="1"/>
                                            <Style.Triggers>
                                                <DataTrigger Binding="{Binding HasEmailError}" Value="True">
                                                    <Setter Property="BorderBrush" Value="Red"/>
                                                    <Setter Property="BorderThickness" Value="1"/>
                                                </DataTrigger>
                                            </Style.Triggers>
                                        </Style>
                                    </Border.Style>
                                    <TextBox Text="{Binding Email, UpdateSourceTrigger=LostFocus}" Background="Transparent" BorderThickness="0" VerticalContentAlignment="Center" Padding="8,0,8,0"/>
                                </Border>
                                <TextBlock Text="{Binding EmailErrorMessage}" Foreground="Red" FontSize="11" FontStyle="Italic" Visibility="{Binding HasEmailError, Converter={StaticResource BoolToVis}}" Margin="5,2,0,0" TextWrapping="Wrap"/>
                            </StackPanel>

                        </StackPanel>
                    </Border>
                </Grid>

                <TextBlock Text="{Binding StatusMessage}" FontWeight="Bold" FontSize="20" TextWrapping="Wrap" VerticalAlignment="Center" HorizontalAlignment="Center" TextAlignment="Center" Margin="0,35,0,0">
                    <TextBlock.Style>
                        <Style TargetType="TextBlock">
                            <Setter Property="Foreground" Value="#e74a3b"/>
                            <Style.Triggers>
                                <DataTrigger Binding="{Binding IsSuccessStatus}" Value="True">
                                    <Setter Property="Foreground" Value="#1cc88a"/>
                                </DataTrigger>
                            </Style.Triggers>
                        </Style>
                    </TextBlock.Style>
                </TextBlock>
            </StackPanel>


            <StackPanel Grid.Column="1" Margin="10,0,0,0">
                <Grid>
                    <Grid.Effect>
                        <DropShadowEffect Color="#FFFFFF" Direction="315" ShadowDepth="2" BlurRadius="15" Opacity="0.15"/>
                    </Grid.Effect>

                    <Border Background="#60FFFFFF" BorderBrush="White" BorderThickness="2" CornerRadius="15" Padding="15">
                        <StackPanel>
                            <TextBlock Text="Thông tin yêu cầu" FontSize="20" FontWeight="Bold" Foreground="#3b5998" Margin="0,0,0,15"/>

                            <Border Background="#fcfcfc" CornerRadius="10" Padding="15" Margin="0,0,0,15">
                                <Grid>
                                    <Grid.RowDefinitions>
                                        <RowDefinition Height="Auto"/>
                                        <RowDefinition Height="10"/>
                                        <RowDefinition Height="Auto"/>
                                    </Grid.RowDefinitions>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="Auto"/>
                                        <ColumnDefinition Width="*"/>
                                    </Grid.ColumnDefinitions>

                                    <TextBlock Text="Mã yêu cầu" Grid.Row="0" Grid.Column="0" Foreground="#FF555555" FontSize="13"/>
                                    <TextBlock Text="{Binding MaYeuCau}" Grid.Row="0" Grid.Column="1" HorizontalAlignment="Right" FontWeight="SemiBold" FontSize="13"/>

                                    <TextBlock Text="Thời gian tạo đơn" Grid.Row="2" Grid.Column="0" Foreground="#FF555555" FontSize="13"/>
                                    <TextBlock Text="{Binding ThoiGian}" Grid.Row="2" Grid.Column="1" HorizontalAlignment="Right" FontWeight="SemiBold" FontSize="13"/>
                                </Grid>
                            </Border>

                            <StackPanel Margin="0,0,0,15">
                                <TextBlock Text="ĐỊA CHỈ ĐĂNG KÝ" FontWeight="SemiBold" Margin="0,0,0,5" Foreground="Black" FontSize="13"/>
                                <Border CornerRadius="12.5" Height="73" Background="White">
                                    <Border.Style>
                                        <Style TargetType="Border">
                                            <Setter Property="BorderBrush" Value="#FFDDDDDD"/>
                                            <Setter Property="BorderThickness" Value="1"/>
                                            <Style.Triggers>
                                                <DataTrigger Binding="{Binding HasDiaChiError}" Value="True">
                                                    <Setter Property="BorderBrush" Value="Red"/>
                                                    <Setter Property="BorderThickness" Value="1"/>
                                                </DataTrigger>
                                            </Style.Triggers>
                                        </Style>
                                    </Border.Style>
                                    <TextBox Text="{Binding DiaChi, UpdateSourceTrigger=LostFocus}" Background="Transparent" BorderThickness="0" TextWrapping="Wrap" AcceptsReturn="True" Padding="8,8,8,8"/>
                                </Border>
                                <TextBlock Text="{Binding DiaChiErrorMessage}" Foreground="Red" FontSize="11" FontStyle="Italic" Visibility="{Binding HasDiaChiError, Converter={StaticResource BoolToVis}}" Margin="5,2,0,0" TextWrapping="Wrap"/>
                            </StackPanel>

                            <Grid Margin="0,3,0,5">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="1*"/>
                                    <ColumnDefinition Width="1*"/>
                                </Grid.ColumnDefinitions>

                                <StackPanel Grid.Column="0" Margin="0,0,10,0">
                                    <TextBlock Text="MỤC ĐÍCH SỬ DỤNG" FontWeight="SemiBold" Margin="0,0,0,5" Foreground="Black" FontSize="13"/>

                                    <Border CornerRadius="12.5" Height="30" Background="White" BorderBrush="#FFDDDDDD" BorderThickness="1" Padding="4,4">

                                        <ComboBox ItemsSource="{Binding MucDichList}" DisplayMemberPath="TenMucDich" SelectedValuePath="MaMucDich" 
                                                  SelectedValue="{Binding SelectedMucDich}" Background="Transparent" BorderThickness="0" 
                                                  VerticalContentAlignment="Center" Margin="5,0,0,0" IsEditable="True" IsReadOnly="True" ForceCursor="True" Cursor="Arrow">
                                            <ComboBox.Resources>
                                                <Style TargetType="TextBox">
                                                    <Setter Property="Cursor" Value="Arrow"/>
                                                    <Setter Property="IsReadOnlyCaretVisible" Value="False"/>
                                                    <Setter Property="ForceCursor" Value="True"/>
                                                </Style>
                                            </ComboBox.Resources>
                                        </ComboBox>
                                    </Border>
                                </StackPanel>

                                <StackPanel Grid.Column="1" Margin="10,0,0,0">
                                    <TextBlock Text="SỐ PHA" FontWeight="SemiBold" Margin="0,0,0,5" Foreground="Black" FontSize="13"/>

                                    <Border CornerRadius="12.5" Height="30" Background="White" BorderBrush="#FFDDDDDD" BorderThickness="1" Padding="4,4">

                                        <ComboBox ItemsSource="{Binding SoPhaList}" DisplayMemberPath="TenSoPha" SelectedValuePath="MaSoPha" 
                                                  SelectedValue="{Binding SelectedSoPha}" Background="Transparent" BorderThickness="0" 
                                                  VerticalContentAlignment="Center" Margin="5,0,0,0" IsEditable="True" IsReadOnly="True" ForceCursor="True" Cursor="Arrow">
                                            <ComboBox.Resources>
                                                <Style TargetType="TextBox">
                                                    <Setter Property="Cursor" Value="Arrow"/>
                                                    <Setter Property="IsReadOnlyCaretVisible" Value="False"/>
                                                    <Setter Property="ForceCursor" Value="True"/>
                                                </Style>
                                            </ComboBox.Resources>
                                        </ComboBox>
                                    </Border>
                                </StackPanel>
                            </Grid>

                        </StackPanel>
                    </Border>
                </Grid>

                <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,35,0,0">
                    <Button Content="HỦY" Command="{Binding HuyCommand}" FontWeight="Bold" Width="80" Height="40" Background="White" Margin="0,0,10,0" BorderThickness="0" Cursor="Hand">
                        <Button.Resources>
                            <Style TargetType="Border">
                                <Setter Property="CornerRadius" Value="17.5"/>
                            </Style>
                        </Button.Resources>

                        <Button.Effect>
                            <DropShadowEffect Color="Black" Direction="270" ShadowDepth="2" BlurRadius="10" Opacity="0.3"/>
                        </Button.Effect>

                    </Button>

                    <Button Content="GỬI ĐỀ NGHỊ ⌲" Command="{Binding GuiCommand}" FontWeight="Bold" Width="140" Height="40" Background="#4e73df" Foreground="White" BorderThickness="0" Cursor="Hand">
                        <Button.Resources>
                            <Style TargetType="Border">
                                <Setter Property="CornerRadius" Value="17.5"/>
                            </Style>
                        </Button.Resources>

                        <Button.Effect>
                            <DropShadowEffect Color="Black" Direction="270" ShadowDepth="2" BlurRadius="10" Opacity="0.3"/>
                        </Button.Effect>

                    </Button>
                </StackPanel>
            </StackPanel>

        </Grid>
    </Grid>
</UserControl>