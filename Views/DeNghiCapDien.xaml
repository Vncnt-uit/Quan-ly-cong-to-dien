<UserControl x:Class="Quản_lý_công_tơ_điện.Views.DeNghiCapDien"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="750" d:DesignWidth="950"
             Background="Transparent">

    <UserControl.Resources>
        <BooleanToVisibilityConverter x:Key="BoolToVis"/>
        <Style x:Key="NoCursorTextBox" TargetType="TextBox">
            <Setter Property="Cursor" Value="Arrow"/>
        </Style>
    </UserControl.Resources>

    <Grid VerticalAlignment="Center" Margin="0,0,0,40">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" Margin="0,0,0,20" HorizontalAlignment="Center">
            <TextBlock FontSize="36" FontWeight="Bold" HorizontalAlignment="Center" Margin="0,0,0,5">
                <TextBlock.Foreground>
                    <LinearGradientBrush StartPoint="0.5,0" EndPoint="0.5,1">
                        <GradientStop Color="#305CF7" Offset="0"/>
                        <GradientStop Color="#7F9EFB" Offset="0.6"/>
                        <GradientStop Color="#CCE1FF" Offset="1"/>
                    </LinearGradientBrush>
                </TextBlock.Foreground>
                <Run Text="Tiếp nhận đề nghị cấp điện"/>
            </TextBlock>
            <TextBlock Text="Vui lòng cung cấp đầy đủ thông tin để tiến hành yêu cầu cấp điện." FontSize="14" Foreground="#475569" HorizontalAlignment="Center" FontWeight="SemiBold"/>
        </StackPanel>

        <Grid Grid.Row="1" Margin="20">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="1*"/>
                <ColumnDefinition Width="1*"/>
            </Grid.ColumnDefinitions>

            <StackPanel Grid.Column="0" Margin="0,0,10,0">
                <Grid>
                    <Grid.Effect>
                        <DropShadowEffect Color="#FFFFFF" Direction="315" ShadowDepth="2" BlurRadius="15" Opacity="0.15"/>
                    </Grid.Effect>

                    <Border Background="#60FFFFFF" BorderBrush="White" BorderThickness="2" CornerRadius="15" Padding="15">
                        <StackPanel>
                            <TextBlock Text="Thông tin khách hàng" FontSize="25" FontWeight="SemiBold" Foreground="#545997" Margin="0,0,0,25"/>

                            <StackPanel Margin="0,0,0,15">
                                <TextBlock Text="SỐ CCCD" FontWeight="Bold" Margin="0,0,0,5" Foreground="#334155" FontSize="11" FontFamily="Public Sans"/>
                                <Border CornerRadius="12.5" Height="35" Background="White">
                                    <Border.Style>
                                        <Style TargetType="Border">
                                            <Setter Property="BorderBrush" Value="#FFDDDDDD"/>
                                            <Setter Property="BorderThickness" Value="1"/>
                                            <Style.Triggers>
                                                <DataTrigger Binding="{Binding HasCccdError}" Value="True">
                                                    <Setter Property="BorderBrush" Value="Red"/>
                                                    <Setter Property="BorderThickness" Value="1"/>
                                                </DataTrigger>
                                            </Style.Triggers>
                                        </Style>
                                    </Border.Style>
                                    <TextBox Text="{Binding Cccd, UpdateSourceTrigger=LostFocus}" Background="Transparent" BorderThickness="0" VerticalContentAlignment="Center" Padding="8,0,8,0"/>
                                </Border>
                                <TextBlock Text="{Binding CccdErrorMessage}" Foreground="Red" FontSize="11" FontStyle="Italic" Visibility="{Binding HasCccdError, Converter={StaticResource BoolToVis}}" Margin="5,2,0,0" TextWrapping="Wrap"/>
                            </StackPanel>

                            <StackPanel Margin="0,0,0,15">
                                <TextBlock Text="HỌ VÀ TÊN" FontWeight="Bold" Margin="0,5,0,5" Foreground="#334155" FontSize="11" FontFamily="Public Sans"/>
                                <Border CornerRadius="12.5" Height="35" Background="White">
                                    <Border.Style>
                                        <Style TargetType="Border">
                                            <Setter Property="BorderBrush" Value="#FFDDDDDD"/>
                                            <Setter Property="BorderThickness" Value="1"/>
                                            <Style.Triggers>
                                                <DataTrigger Binding="{Binding HasHoTenError}" Value="True">
                                                    <Setter Property="BorderBrush" Value="Red"/>
                                                    <Setter Property="BorderThickness" Value="1"/>
                                                </DataTrigger>
                                            </Style.Triggers>
                                        </Style>
                                    </Border.Style>
                                    <TextBox Text="{Binding HoTen, UpdateSourceTrigger=LostFocus}" Background="Transparent" BorderThickness="0" VerticalContentAlignment="Center" Padding="8,0,8,0"/>
                                </Border>
                                <TextBlock Text="{Binding HoTenErrorMessage}" Foreground="Red" FontSize="11" FontStyle="Italic" Visibility="{Binding HasHoTenError, Converter={StaticResource BoolToVis}}" Margin="5,2,0,0" TextWrapping="Wrap"/>
                            </StackPanel>

                            <Grid Margin="0,0,0,15">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="1*"/>
                                    <ColumnDefinition Width="1*"/>
                                </Grid.ColumnDefinitions>

                                <StackPanel Grid.Column="0" Margin="0,0,10,0">
                                    <TextBlock Text="NĂM SINH" FontWeight="Bold" Margin="0,5,0,5" Foreground="#334155" FontSize="12" FontFamily="Public Sans"/>

                                    <Border CornerRadius="12.5" Height="37" Background="White">
                                        <Border.Style>
                                            <Style TargetType="Border">
                                                <Setter Property="BorderBrush" Value="#FFDDDDDD"/>
                                                <Setter Property="BorderThickness" Value="1"/>
                                                <Style.Triggers>
                                                    <DataTrigger Binding="{Binding HasNamSinhError}" Value="True">
                                                        <Setter Property="BorderBrush" Value="Red"/>
                                                        <Setter Property="BorderThickness" Value="1"/>
                                                    </DataTrigger>
                                                </Style.Triggers>
                                            </Style>
                                        </Border.Style>
                                        <TextBox Text="{Binding NamSinh, UpdateSourceTrigger=LostFocus, TargetNullValue=''}" Background="Transparent" BorderThickness="0" VerticalContentAlignment="Center" Padding="8,0,8,0"/>
                                    </Border>

                                    <TextBlock Text="{Binding NamSinhErrorMessage}" Foreground="Red" FontSize="11" FontStyle="Italic" Visibility="{Binding HasNamSinhError, Converter={StaticResource BoolToVis}}" Margin="5,2,0,0" TextWrapping="Wrap"/>
                                </StackPanel>

                                <StackPanel Grid.Column="1" Margin="10,0,0,0">
                                    <TextBlock Text="SỐ ĐIỆN THOẠI" FontWeight="Bold" Margin="0,5,0,5" Foreground="#334155" FontSize="12" FontFamily="Public Sans"/>
                                    <Border CornerRadius="12.5" Height="37" Background="White">
                                        <Border.Style>
                                            <Style TargetType="Border">
                                                <Setter Property="BorderBrush" Value="#FFDDDDDD"/>
                                                <Setter Property="BorderThickness" Value="1"/>
                                                <Style.Triggers>
                                                    <DataTrigger Binding="{Binding HasSdtError}" Value="True">
                                                        <Setter Property="BorderBrush" Value="Red"/>
                                                        <Setter Property="BorderThickness" Value="1"/>
                                                    </DataTrigger>
                                                </Style.Triggers>
                                            </Style>
                                        </Border.Style>
                                        <TextBox Text="{Binding SoDienThoai, UpdateSourceTrigger=LostFocus}" Background="Transparent" BorderThickness="0" VerticalContentAlignment="Center" Padding="8,0,8,0"/>
                                    </Border>
                                    <TextBlock Text="{Binding SdtErrorMessage}" Foreground="Red" FontSize="11" FontStyle="Italic" Visibility="{Binding HasSdtError, Converter={StaticResource BoolToVis}}" Margin="5,2,0,0" TextWrapping="Wrap"/>
                                </StackPanel>
                            </Grid>

                            <StackPanel Margin="0,0,0,5">
                                <TextBlock Text="EMAIL" FontWeight="Bold" Margin="0,5,0,5" Foreground="#334155" FontSize="12" FontFamily="Public Sans"/>
                                <Border CornerRadius="12.5" Height="35" Background="White">
                                    <Border.Style>
                                        <Style TargetType="Border">
                                            <Setter Property="BorderBrush" Value="#FFDDDDDD"/>
                                            <Setter Property="BorderThickness" Value="1"/>
                                            <Style.Triggers>
                                                <DataTrigger Binding="{Binding HasEmailError}" Value="True">
                                                    <Setter Property="BorderBrush" Value="Red"/>
                                                    <Setter Property="BorderThickness" Value="1"/>
                                                </DataTrigger>
                                            </Style.Triggers>
                                        </Style>
                                    </Border.Style>
                                    <TextBox Text="{Binding Email, UpdateSourceTrigger=LostFocus}" Background="Transparent" BorderThickness="0" VerticalContentAlignment="Center" Padding="8,0,8,0"/>
                                </Border>
                                <TextBlock Text="{Binding EmailErrorMessage}" Foreground="Red" FontSize="11" FontStyle="Italic" Visibility="{Binding HasEmailError, Converter={StaticResource BoolToVis}}" Margin="5,2,0,0" TextWrapping="Wrap"/>
                            </StackPanel>

                        </StackPanel>
                    </Border>
                </Grid>

                <TextBlock Text="{Binding StatusMessage}" FontWeight="Bold" FontSize="20" TextWrapping="Wrap" VerticalAlignment="Center" HorizontalAlignment="Center" TextAlignment="Center" Margin="0,35,0,0">
                    <TextBlock.Style>
                        <Style TargetType="TextBlock">
                            <Setter Property="Foreground" Value="#e74a3b"/>
                            <Style.Triggers>
                                <DataTrigger Binding="{Binding IsSuccessStatus}" Value="True">
                                    <Setter Property="Foreground" Value="#1cc88a"/>
                                </DataTrigger>
                            </Style.Triggers>
                        </Style>
                    </TextBlock.Style>
                </TextBlock>
            </StackPanel>


            <StackPanel Grid.Column="1" Margin="10,0,0,0">
                <Grid>
                    <Grid.Effect>
                        <DropShadowEffect Color="#FFFFFF" Direction="315" ShadowDepth="2" BlurRadius="15" Opacity="0.15"/>
                    </Grid.Effect>

                    <Border Background="#60FFFFFF" BorderBrush="White" BorderThickness="2" CornerRadius="15" Padding="15">
                        <StackPanel>
                            <TextBlock Text="Thông tin yêu cầu" FontSize="25" FontWeight="SemiBold" Foreground="#545997" Margin="0,0,0,25" FontFamily="Public Sans"/>

                            <StackPanel Margin="0,0,0,15">
                                <TextBlock Text="MÃ PHIẾU CẤP ĐIỆN" FontWeight="Bold" Margin="0,5,0,5" Foreground="#334155" FontSize="12" FontFamily="Public Sans"/>

                                <Border CornerRadius="12.5" Height="35" Background="#E9ECEF" BorderBrush="#FFDDDDDD" BorderThickness="1">
                                    <TextBox Text="{Binding MaYeuCau, Mode=OneWay}" IsReadOnly="True" Background="Transparent" BorderThickness="0" VerticalContentAlignment="Center" Padding="8,0,8,0" Foreground="#FF555555"/>
                                </Border>
                            </StackPanel>

                            <StackPanel Margin="0,0,0,15">
                                <TextBlock Text="THỜI GIAN GỬI YÊU CẦU" FontWeight="Bold" Margin="0,0,0,5" Foreground="#334155" FontSize="12" FontFamily="Public Sans"/>
                                <Border CornerRadius="12.5" Height="33" Background="#E9ECEF" BorderBrush="#FFDDDDDD" BorderThickness="1">
                                    <Grid>
                                        <TextBox Text="{Binding ThoiGian, Mode=OneWay, StringFormat='dd/MM/yyyy - HH:mm:ss'}" IsReadOnly="True" Background="Transparent" BorderThickness="0" VerticalContentAlignment="Center" Padding="8,0,30,0" Foreground="#FF555555"/>
                                        <TextBlock Text="📅" FontSize="14" HorizontalAlignment="Right" VerticalAlignment="Center" Margin="0,0,10,0" Opacity="0.6" IsHitTestVisible="False"/>
                                    </Grid>
                                </Border>
                            </StackPanel>

                            <StackPanel Margin="0,0,0,15">
                                <TextBlock Text="ĐỊA CHỈ ĐĂNG KÝ" FontWeight="Bold" Margin="0,0,0,5" Foreground="#334155" FontSize="12" FontFamily="Public Sans"/>
                                <Border CornerRadius="12.5" Height="50" Background="White">
                                    <Border.Style>
                                        <Style TargetType="Border">
                                            <Setter Property="BorderBrush" Value="#FFDDDDDD"/>
                                            <Setter Property="BorderThickness" Value="1"/>
                                            <Style.Triggers>
                                                <DataTrigger Binding="{Binding HasDiaChiError}" Value="True">
                                                    <Setter Property="BorderBrush" Value="Red"/>
                                                    <Setter Property="BorderThickness" Value="1"/>
                                                </DataTrigger>
                                            </Style.Triggers>
                                        </Style>
                                    </Border.Style>

                                    <TextBox Text="{Binding DiaChi, UpdateSourceTrigger=LostFocus}" Background="Transparent" BorderThickness="0" TextWrapping="Wrap" AcceptsReturn="True"  VerticalContentAlignment="Top" Padding="8,8,8,8"/>
                                </Border>
                                <TextBlock Text="{Binding DiaChiErrorMessage}" Foreground="Red" FontSize="11" FontStyle="Italic" Visibility="{Binding HasDiaChiError, Converter={StaticResource BoolToVis}}" Margin="5,2,0,0" TextWrapping="Wrap"/>
                            </StackPanel>

                            <Grid Margin="0,3,0,5">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="1*"/>
                                    <ColumnDefinition Width="1*"/>
                                </Grid.ColumnDefinitions>

                                <StackPanel Grid.Column="0" Margin="0,0,10,0">
                                    <TextBlock Text="MỤC ĐÍCH SỬ DỤNG" FontWeight="Bold" Margin="0,0,0,5" Foreground="#334155" FontSize="12" FontFamily="Public Sans"/>

                                    <Border CornerRadius="12.5" Height="32" Background="White" Padding="4,4">
                                        <Border.Style>
                                            <Style TargetType="Border">
                                                <Setter Property="BorderBrush" Value="#FFDDDDDD"/>
                                                <Setter Property="BorderThickness" Value="1"/>
                                                <Style.Triggers>
                                                    <DataTrigger Binding="{Binding HasMucDichError}" Value="True">
                                                        <Setter Property="BorderBrush" Value="Red"/>
                                                    </DataTrigger>
                                                </Style.Triggers>
                                            </Style>
                                        </Border.Style>

                                        <ComboBox ItemsSource="{Binding MucDichList}" DisplayMemberPath="TenMucDich" SelectedValuePath="MaMucDich" 
                                                  SelectedValue="{Binding SelectedMucDich, UpdateSourceTrigger=PropertyChanged}" Background="Transparent" BorderThickness="0" 
                                                  VerticalContentAlignment="Center" Margin="5,0,0,0" IsEditable="True" 
                                                  IsReadOnly="True" ForceCursor="True" Cursor="Arrow">
                                            <ComboBox.Resources>
                                                <Style TargetType="TextBox">
                                                    <Setter Property="Cursor" Value="Arrow"/>
                                                    <Setter Property="IsReadOnlyCaretVisible" Value="False"/>
                                                    <Setter Property="ForceCursor" Value="True"/>
                                                </Style>
                                            </ComboBox.Resources>
                                        </ComboBox>
                                    </Border>
                                    <TextBlock Text="{Binding MucDichErrorMessage}" Foreground="Red" FontSize="11" FontStyle="Italic" Visibility="{Binding HasMucDichError, Converter={StaticResource BoolToVis}}" Margin="5,2,0,0" TextWrapping="Wrap"/>
                                </StackPanel>

                                <StackPanel Grid.Column="1" Margin="10,0,0,0">
                                    <TextBlock Text="SỐ PHA" FontWeight="Bold" Margin="0,0,0,5" Foreground="#334155" FontSize="12" FontFamily="Public Sans"/>

                                    <Border CornerRadius="12.5" Height="32" Background="White" Padding="4,4">
                                        <Border.Style>
                                            <Style TargetType="Border">
                                                <Setter Property="BorderBrush" Value="#FFDDDDDD"/>
                                                <Setter Property="BorderThickness" Value="1"/>
                                                <Style.Triggers>
                                                    <DataTrigger Binding="{Binding HasSoPhaError}" Value="True">
                                                        <Setter Property="BorderBrush" Value="Red"/>
                                                    </DataTrigger>
                                                </Style.Triggers>
                                            </Style>
                                        </Border.Style>

                                        <ComboBox ItemsSource="{Binding SoPhaList}" DisplayMemberPath="TenSoPha" SelectedValuePath="MaSoPha" 
                                                  SelectedValue="{Binding SelectedSoPha, UpdateSourceTrigger=PropertyChanged}" Background="Transparent" BorderThickness="0" 
                                                  VerticalContentAlignment="Center" Margin="5,0,0,0" IsEditable="True" 
                                                  IsReadOnly="True" ForceCursor="True" Cursor="Arrow">
                                            <ComboBox.Resources>
                                                <Style TargetType="TextBox">
                                                    <Setter Property="Cursor" Value="Arrow"/>
                                                    <Setter Property="IsReadOnlyCaretVisible" Value="False"/>
                                                    <Setter Property="ForceCursor" Value="True"/>
                                                </Style>
                                            </ComboBox.Resources>
                                        </ComboBox>
                                    </Border>
                                    <TextBlock Text="{Binding SoPhaErrorMessage}" Foreground="Red" FontSize="11" FontStyle="Italic" Visibility="{Binding HasSoPhaError, Converter={StaticResource BoolToVis}}" Margin="5,2,0,0" TextWrapping="Wrap"/>
                                </StackPanel>
                            </Grid>

                        </StackPanel>
                    </Border>
                </Grid>

                <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,35,0,0">
                    <Button Content="✕  THOÁT" Command="{Binding ThoatCommand}" FontWeight="Bold" Width="120" Height="40" Background="White" Margin="0,0,10,0" BorderThickness="0" Cursor="Hand">
                        <Button.Resources>
                            <Style TargetType="Border">
                                <Setter Property="CornerRadius" Value="17.5"/>
                            </Style>
                        </Button.Resources>

                        <Button.Effect>
                            <DropShadowEffect Color="Black" Direction="270" ShadowDepth="2" BlurRadius="10" Opacity="0.3"/>
                        </Button.Effect>

                    </Button>

                    <Button Content="GỬI ĐỀ NGHỊ  ⌲" Command="{Binding GuiCommand}" FontWeight="Bold" Width="160" Height="40" Background="#4A72FF" Foreground="White" BorderThickness="0" Cursor="Hand">
                        <Button.Resources>
                            <Style TargetType="Border">
                                <Setter Property="CornerRadius" Value="17.5"/>
                            </Style>
                        </Button.Resources>

                        <Button.Effect>
                            <DropShadowEffect Color="Black" Direction="270" ShadowDepth="2" BlurRadius="10" Opacity="0.3"/>
                        </Button.Effect>

                    </Button>
                </StackPanel>
            </StackPanel>

        </Grid>
    </Grid>
</UserControl>