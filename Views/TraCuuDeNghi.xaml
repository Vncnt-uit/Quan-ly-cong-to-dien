<UserControl x:Class="Quản_lý_công_tơ_điện.Views.TraCuuDeNghi"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="750" d:DesignWidth="1100" 
             Background="Transparent">

    <UserControl.Resources>
        <Style x:Key="CenterText" TargetType="TextBlock">
            <Setter Property="HorizontalAlignment" Value="Center"/>
            <Setter Property="VerticalAlignment" Value="Center"/>
            <Setter Property="TextWrapping" Value="Wrap"/>
        </Style>

        <Style TargetType="DataGridRow">
            <Style.Resources>
                <SolidColorBrush x:Key="{x:Static SystemColors.HighlightBrushKey}" Color="#cbd5e1"/>
                <SolidColorBrush x:Key="{x:Static SystemColors.InactiveSelectionHighlightBrushKey}" Color="#cbd5e1"/>
                <SolidColorBrush x:Key="{x:Static SystemColors.HighlightTextBrushKey}" Color="Black"/>
            </Style.Resources>
        </Style>

        <Style x:Key="VerticalCenterText" TargetType="TextBlock">
            <Setter Property="VerticalAlignment" Value="Center"/>
            <Setter Property="Margin" Value="15,0,15,0"/>
            <Setter Property="TextWrapping" Value="Wrap"/>
        </Style>

        <Style TargetType="DataGridColumnHeader">
            <Setter Property="Background" Value="#d6e4ff"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="Foreground" Value="#001A42"/>
            <Setter Property="MinHeight" Value="45"/>
            <Setter Property="HorizontalContentAlignment" Value="Center"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
            <Setter Property="BorderBrush" Value="#bacdf2"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Padding" Value="5"/>
            <Setter Property="ContentTemplate">
                <Setter.Value>
                    <DataTemplate>
                        <TextBlock Text="{Binding}" TextWrapping="Wrap" TextAlignment="Center"/>
                    </DataTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style TargetType="DatePickerTextBox">
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="DatePickerTextBox">
                        <TextBox x:Name="PART_TextBox" 
                         Text="{Binding Path=SelectedDate, RelativeSource={RelativeSource AncestorType={x:Type DatePicker}}, StringFormat=dd/MM/yyyy}" 
                         BorderThickness="0" 
                         Background="Transparent" 
                         VerticalContentAlignment="Center" 
                         Padding="5,0"/>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </UserControl.Resources>

    <Grid VerticalAlignment="Center" Margin="30">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" Margin="0,0,0,30" HorizontalAlignment="Center">
            <TextBlock FontSize="36" FontWeight="Bold" HorizontalAlignment="Center" Margin="0,0,0,10">
                <TextBlock.Foreground>
                    <LinearGradientBrush StartPoint="0.5,0" EndPoint="0.5,1">
                        <GradientStop Color="#305CF7" Offset="0"/>
                        <GradientStop Color="#7F9EFB" Offset="0.6"/>
                        <GradientStop Color="#CCE1FF" Offset="1"/>
                    </LinearGradientBrush>
                </TextBlock.Foreground>
                <Run Text="Tra cứu đề nghị cấp điện"/>
            </TextBlock>
            <TextBlock Text="Nhập mã đề nghị của bạn để theo dõi tiến độ xử lý và cập nhật thông tin mới nhất từ điện lực." 
                       FontSize="14" Foreground="#464555" HorizontalAlignment="Center" FontWeight="SemiBold"/>
        </StackPanel>

        <Border Grid.Row="1" Background="#60FFFFFF" BorderBrush="#ffffff" BorderThickness="2" CornerRadius="15" Padding="25">
            <Border.Effect>
                <DropShadowEffect Color="#000000" Direction="270" ShadowDepth="4" BlurRadius="20" Opacity="0.08"/>
            </Border.Effect>

            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <TextBlock Grid.Row="0" Text="Tra cứu đề nghị cấp điện" FontSize="26" FontWeight="SemiBold" Foreground="#545997" Margin="0,0,0,20"/>

                <Grid Grid.Row="1" Margin="0,0,0,20">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="1*"/>
                        <ColumnDefinition Width="1*"/>
                        <ColumnDefinition Width="1*"/>
                        <ColumnDefinition Width="1*"/>
                        <ColumnDefinition Width="1*"/>
                        <ColumnDefinition Width="1*"/>
                    </Grid.ColumnDefinitions>

                    <StackPanel Grid.Column="0" Margin="0,0,10,0">
                        <TextBlock Text="Mã phiếu cấp điện" FontWeight="SemiBold" Foreground="#464555" FontSize="12" Margin="0,0,0,8" HorizontalAlignment="Center"/>
                        <Border CornerRadius="16" Height="32" Background="White" BorderBrush="#e0e0e0" BorderThickness="1">
                            <TextBox Text="{Binding MaPhieu, UpdateSourceTrigger=PropertyChanged}" Background="Transparent" BorderThickness="0" Padding="12,0" VerticalContentAlignment="Center"/>
                        </Border>
                    </StackPanel>

                    <StackPanel Grid.Column="1" Margin="5,0,10,0">
                        <TextBlock Text="Họ tên khách hàng" FontWeight="SemiBold" Foreground="#464555" FontSize="12" Margin="0,0,0,8" HorizontalAlignment="Center"/>
                        <Border CornerRadius="16" Height="32" Background="White" BorderBrush="#e0e0e0" BorderThickness="1">
                            <TextBox Text="{Binding HoTen, UpdateSourceTrigger=PropertyChanged}" Background="Transparent" BorderThickness="0" Padding="12,0" VerticalContentAlignment="Center"/>
                        </Border>
                    </StackPanel>

                    <StackPanel Grid.Column="2" Margin="5,0,10,0">
                        <TextBlock Text="Địa chỉ đăng ký" FontWeight="SemiBold" Foreground="#464555" FontSize="12" Margin="0,0,0,8" HorizontalAlignment="Center"/>
                        <Border CornerRadius="16" Height="32" Background="White" BorderBrush="#e0e0e0" BorderThickness="1">
                            <TextBox Text="{Binding DiaChi, UpdateSourceTrigger=PropertyChanged}" Background="Transparent" BorderThickness="0" Padding="12,0" VerticalContentAlignment="Center"/>
                        </Border>
                    </StackPanel>

                    <StackPanel Grid.Column="3" Margin="5,0,10,0">
                        <TextBlock Text="Số pha đăng ký" FontWeight="SemiBold" Foreground="#464555" FontSize="12" Margin="0,0,0,8" HorizontalAlignment="Center"/>
                        <Border CornerRadius="16" Height="32" Background="White" BorderBrush="#e0e0e0" BorderThickness="1" Padding="5,5">
                            <ComboBox ItemsSource="{Binding SoPhaList}" DisplayMemberPath="TenSoPha" SelectedValuePath="MaSoPha" SelectedValue="{Binding SelectedSoPha}" Background="Transparent" BorderThickness="0" VerticalContentAlignment="Center" IsEditable="True" IsReadOnly="True" Cursor="Arrow" ForceCursor="True">
                                <ComboBox.Resources>
                                    <Style TargetType="TextBox">
                                        <Setter Property="Cursor" Value="Arrow"/>
                                        <Setter Property="IsReadOnlyCaretVisible" Value="False"/>
                                        <Setter Property="ForceCursor" Value="True"/>
                                    </Style>
                                </ComboBox.Resources>
                            </ComboBox>
                        </Border>
                    </StackPanel>

                    <StackPanel Grid.Column="4" Margin="5,0,10,0">
                        <TextBlock Text="Mục đích sử dụng" FontWeight="SemiBold" Foreground="#464555" FontSize="12" Margin="0,0,0,8" HorizontalAlignment="Center"/>
                        <Border CornerRadius="16" Height="32" Background="White" BorderBrush="#e0e0e0" BorderThickness="1" Padding="5,5">
                            <ComboBox ItemsSource="{Binding MucDichList}" DisplayMemberPath="TenMucDich" SelectedValuePath="MaMucDich" SelectedValue="{Binding SelectedMucDich}" Background="Transparent" BorderThickness="0" VerticalContentAlignment="Center" IsEditable="True" IsReadOnly="True" Cursor="Arrow" ForceCursor="True">
                                <ComboBox.Resources>
                                    <Style TargetType="TextBox">
                                        <Setter Property="Cursor" Value="Arrow"/>
                                        <Setter Property="IsReadOnlyCaretVisible" Value="False"/>
                                        <Setter Property="ForceCursor" Value="True"/>
                                    </Style>
                                </ComboBox.Resources>
                            </ComboBox>
                        </Border>
                    </StackPanel>

                    <StackPanel Grid.Column="5" Margin="5,0,0,0">
                        <TextBlock Text="Trạng thái" FontWeight="SemiBold" Foreground="#464555" FontSize="12" Margin="0,0,0,8" HorizontalAlignment="Center"/>
                        <Border CornerRadius="16" Height="32" Background="White" BorderBrush="#e0e0e0" BorderThickness="1">
                            <TextBox Text="{Binding TrangThai, UpdateSourceTrigger=PropertyChanged}" Background="Transparent" BorderThickness="0" Padding="12,0" VerticalContentAlignment="Center"/>
                        </Border>
                    </StackPanel>
                </Grid>

                <Grid Grid.Row="2" Margin="0,0,0,25">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>
                    <StackPanel Grid.Column="0" Orientation="Horizontal" VerticalAlignment="Center">
                        <TextBlock Text="Thời gian gửi yêu cầu" FontWeight="SemiBold" Foreground="#464555" FontSize="12" VerticalAlignment="Center" Margin="0,0,15,0"/>
                        <TextBlock Text="từ" FontWeight="Bold" Foreground="Red" VerticalAlignment="Center" Margin="0,0,10,0"/>
                        <Border CornerRadius="16" Height="32" Background="White" BorderBrush="#e0e0e0" BorderThickness="1" Padding="5,0" Width="220">
                            <DatePicker SelectedDate="{Binding TuNgay, UpdateSourceTrigger=PropertyChanged}" Background="Transparent" BorderThickness="0" VerticalContentAlignment="Center"/>
                        </Border>
                        <TextBlock Text="đến" FontWeight="Bold" Foreground="Red" VerticalAlignment="Center" Margin="15,0,10,0"/>
                        <Border CornerRadius="16" Height="32" Background="White" BorderBrush="#e0e0e0" BorderThickness="1" Padding="5,0" Width="220">
                            <DatePicker SelectedDate="{Binding DenNgay, UpdateSourceTrigger=PropertyChanged}" Background="Transparent" BorderThickness="0" VerticalContentAlignment="Center"/>
                        </Border>
                    </StackPanel>
                    <StackPanel Grid.Column="1" Orientation="Horizontal" HorizontalAlignment="Right" VerticalAlignment="Center">
                        <Button Command="{Binding TraCuuCommand}" Width="140" Height="36" Background="#4A72FF" Foreground="White" BorderThickness="0" Margin="0,0,15,0" Cursor="Hand">
                            <Button.Resources>
                                <Style TargetType="Border">
                                    <Setter Property="CornerRadius" Value="18"/>
                                </Style>
                            </Button.Resources>
                            <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                                <TextBlock Text="TRA CỨU" FontWeight="Bold" FontSize="13" Margin="0,0,5,0"/>
                                <TextBlock Text="🔍" FontSize="14" Margin="0,-1,0,0"/>
                            </StackPanel>
                        </Button>
                        <Button Content="✕ THOÁT" Command="{Binding ThoatCommand}" Width="120" Height="36" Background="White" FontWeight="Bold" FontSize="13" BorderThickness="0" Cursor="Hand">
                            <Button.Resources>
                                <Style TargetType="Border">
                                    <Setter Property="CornerRadius" Value="18"/>
                                </Style>
                            </Button.Resources>
                        </Button>
                    </StackPanel>
                </Grid>

                <Border Grid.Row="3" VerticalAlignment="Top" Background="#60FFFFFF" BorderBrush="#ffffff" BorderThickness="2" CornerRadius="15" Padding="25">
                    <Border.Effect>
                        <DropShadowEffect Color="#000000" Direction="270" ShadowDepth="4" BlurRadius="20" Opacity="0.08"/>
                    </Border.Effect>
                    <Border.Style>
                        <Style TargetType="Border">
                            <Setter Property="CornerRadius" Value="12"/>
                            <Style.Triggers>
                                <DataTrigger Binding="{Binding ElementName=BM3Grid, Path=HasItems}" Value="False">
                                    <Setter Property="CornerRadius" Value="12,12,3,3"/>
                                </DataTrigger>
                            </Style.Triggers>
                        </Style>
                    </Border.Style>
                    <Grid>
                        <Border x:Name="MaskBM3" Background="White">
                            <Border.Style>
                                <Style TargetType="Border">
                                    <Setter Property="CornerRadius" Value="11"/>
                                    <Style.Triggers>
                                        <DataTrigger Binding="{Binding ElementName=BM3Grid, Path=HasItems}" Value="False">
                                            <Setter Property="CornerRadius" Value="11,11,2,2"/>
                                        </DataTrigger>
                                    </Style.Triggers>
                                </Style>
                            </Border.Style>
                        </Border>
                        <Grid>
                            <Grid.OpacityMask>
                                <VisualBrush Visual="{Binding ElementName=MaskBM3}" Stretch="None" AlignmentX="Left" AlignmentY="Top"/>
                            </Grid.OpacityMask>
                            <Border Background="White"/>
                            <Border Height="46" Background="#d6e4ff" VerticalAlignment="Top" CornerRadius="11,11,0,0"/>
                            <DataGrid x:Name="BM3Grid" ItemsSource="{Binding SearchResults, IsAsync=True}" AutoGenerateColumns="False" IsReadOnly="True" HeadersVisibility="Column" GridLinesVisibility="None" HorizontalGridLinesBrush="#bacdf2" VerticalGridLinesBrush="#bacdf2" Background="Transparent" BorderThickness="0" RowHeight="40" AlternatingRowBackground="#F8F9FF" VerticalContentAlignment="Center" HorizontalScrollBarVisibility="Disabled" VirtualizingPanel.ScrollUnit="Pixel">
                                <DataGrid.Columns>
                                    <DataGridTextColumn Header="STT" Binding="{Binding STT}" Width="60" ElementStyle="{StaticResource CenterText}"/>
                                    <DataGridTextColumn Header="THỜI GIAN GỬI YÊU CẦU" Binding="{Binding ThoiGianGui}" Width="170" ElementStyle="{StaticResource CenterText}"/>
                                    <DataGridTextColumn Header="HỌ TÊN KHÁCH HÀNG" Binding="{Binding HoTen}" Width="1.5*" ElementStyle="{StaticResource VerticalCenterText}"/>
                                    <DataGridTextColumn Header="ĐỊA CHỈ ĐĂNG KÝ" Binding="{Binding DiaChi}" Width="2*" ElementStyle="{StaticResource VerticalCenterText}"/>
                                    <DataGridTextColumn Header="SỐ PHA ĐĂNG KÝ" Binding="{Binding SoPha}" Width="130" ElementStyle="{StaticResource CenterText}"/>
                                    <DataGridTextColumn Header="MỤC ĐÍCH SỬ DỤNG" Binding="{Binding MucDich}" Width="1.5*" ElementStyle="{StaticResource VerticalCenterText}"/>
                                    <DataGridTextColumn Header="TRẠNG THÁI" Binding="{Binding TrangThai}" Width="160">
                                        <DataGridTextColumn.ElementStyle>
                                            <Style TargetType="TextBlock" BasedOn="{StaticResource CenterText}">
                                                <Setter Property="FontWeight" Value="Bold"/>
                                                <Setter Property="Foreground" Value="#e74a3b"/>
                                                <Style.Triggers>
                                                    <DataTrigger Binding="{Binding TrangThai}" Value="Đã xử lý">
                                                        <Setter Property="Foreground" Value="#1cc88a"/>
                                                    </DataTrigger>
                                                </Style.Triggers>
                                            </Style>
                                        </DataGridTextColumn.ElementStyle>
                                    </DataGridTextColumn>
                                </DataGrid.Columns>
                            </DataGrid>
                        </Grid>
                    </Grid>
                </Border>

                <TextBlock Grid.Row="4" Text="{Binding StateMessage}" HorizontalAlignment="Center" VerticalAlignment="Center" FontSize="25" FontWeight="Bold" Margin="0,20,0,0">
                    <TextBlock.Style>
                        <Style TargetType="TextBlock">
                            <Setter Property="Foreground" Value="#e74a3b"/>
                            <Style.Triggers>
                                <DataTrigger Binding="{Binding IsSuccessStatus}" Value="True">
                                    <Setter Property="Foreground" Value="#1cc88a"/>
                                </DataTrigger>
                            </Style.Triggers>
                        </Style>
                    </TextBlock.Style>
                </TextBlock>
            </Grid>
        </Border>
    </Grid>
</UserControl>